/**
 * @format
 */

import 'react-native';
import {calculateTaxAndTotals} from '../../../../src/utils/checkoutUtils';
import {SEATTLE_SALES_TAX_RATE} from '../../../../src/utils/taxRates';

// Note: import explicitly to use the types shipped with jest.
import {it} from '@jest/globals';

describe('calculateTaxAndTotals tests', () => {
  test.todo('create mock cart and run tests');
});
