import {StyleSheet} from 'react-native';

const buttonStyles = StyleSheet.create({
  addToCartButton: {
    backgroundColor: 'blue',
    borderRadius: 20,
    padding: 8,
    elevation: 2,
  },
});

export {buttonStyles};
